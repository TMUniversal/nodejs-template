// const index = require('../dist/index')

describe('a test collection', () => {
  test('a test', () => {
    const res = !!'a test condition'
    const expected = true
    expect(res).toBe(expected)
  })
})
